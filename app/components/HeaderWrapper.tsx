//import { auth } from "@clerk/nextjs/server"; put back when we have a real project
import Header from "./Header";
import { Metadata } from "next";

export default async function HeaderWrapper() {
  //const { userId } = await auth(); put back when we have a real project
  //return <Header userId={userId} />; put back when we have a real project
}